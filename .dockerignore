# All node_modules (rebuilt inside container)
**/node_modules/

# Build outputs (rebuilt by turbo)
**/dist/
**/build/
**/.turbo/

# State/data (volume-mounted, not baked into image)
**/state/

# Logs and env
*.log
.env

# Git and IDE
**/.git/
**/.gitnexus/
.gitmodules

# tsbuildinfo (regenerated by tsc --build)
**/*.tsbuildinfo
